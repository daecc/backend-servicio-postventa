package com.unmsm.marketplace.postventa_service.repository;

import com.unmsm.marketplace.postventa_service.model.LogisticaInversa;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogisticaInversaRepository extends JpaRepository<LogisticaInversa, Long> {
}